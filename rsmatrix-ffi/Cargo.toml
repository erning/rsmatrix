[package]
name = "rsmatrix-ffi"
version = "0.1.0"
edition = "2021"
description = "C FFI bindings for rsmatrix simulation core"

[lib]
crate-type = ["staticlib"]

[dependencies]
rsmatrix-core = { workspace = true }
